"# gitTesting" 

#Crear rama desde comando
git checkout -b branch-1

#Cambiarse de rama
git checkout -b branch-1

#subir cambios al repo remoto: 
git push origin branch

#Cambiarse de rama
git checkout -b branch-1